// src/routes/parameterRoutes.ts
// Routes for parameter management operations
import express from 'express';
import * as parameterController from '../controllers/parameterController';

const router = express.Router();

// TODO: Implement parameter routes
// Example:
// router.get('/', parameterController.getParameters);
// router.get('/:name/valid-values', parameterController.getValidValues);

export default router;